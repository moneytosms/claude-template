---
description: Run the test suite (and report failures concisely).
---

Run the test command from CLAUDE.md's Commands table. If a path/pattern is given as an argument, scope the run to it. Report pass/fail counts and the first real failure with its message. Don't fix anything unless asked.
