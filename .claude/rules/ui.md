# UI conventions

<!-- Loaded on demand. Design + component rules. -->

- Icon library: <your-lib> (NOT lucide).
- Spacing scale: <e.g. 4px base>
- Colors/tokens: <path to design tokens>
- Component patterns: <e.g. composition over props explosion>
- Accessibility: keyboard nav + ARIA on interactive elements.
