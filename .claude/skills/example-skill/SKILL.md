---
name: example-skill
description: Template for a project skill. Replace with your reusable workflow. Trigger via /example-skill.
---

# Example skill

Replace this with a multi-step workflow you repeat often (e.g. "write release notes", "audit a11y", "scaffold a feature").

## Steps
1. <step one>
2. <step two>
3. <output format you want>

Notes:
- Project skills live here (`.claude/skills/`). Global skills live in `~/.claude/skills/` and work in any project.
- Keep one workflow per skill. Bundle many into a plugin if sharing.
